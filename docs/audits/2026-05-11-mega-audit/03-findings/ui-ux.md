# UI/UX Findings — 2026-05-11

Single-file dashboard at `C:\Users\User\Desktop\DEV\github\mabat-443-dashboard\index.html` (9715 lines).
Audit scope: visual layout, contrast, typography, interaction states, accessibility, RTL correctness, animation perf.

## P0 — Critical (blocks use)

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 1 | `index.html:1` (entire file) | **Zero `aria-label`, `role`, or `tabindex` attributes in 9715 lines.** Icon-only buttons (sidebar toggle line 4008, alert checkmarks, map controls 4125–4127, arrest planner close 4133, vote-btn 1113, layer toggle, etc.) are unreadable to screen readers and unreachable to keyboard-only users. The arrest planner's 8 export buttons (4183–4189) read as empty SVG to AT. | Add `aria-label="תפריט ניווט"`, `aria-label="סגור"`, `aria-label="הרחב מפה"` etc. on every icon-only `<button>` and `<a>`. For the sidebar toggle add `aria-expanded` toggled from JS at line 5717. | M |
| 2 | `index.html:1297` (`.history-search`), `:1547` (report fields), `:888` (search input), `:2474` (`.arrest-name-row input`), `:4167` (target name) | **All form inputs are 11–12 px font-size.** iOS Safari auto-zooms when a touched field is <16 px — page becomes unusable after the zoom (layout breaks, RTL flipping, no easy zoom-out). Affects login (18 px, OK), but every other form: report, search, arrest name, corner names. | Bump every text/textarea input to `font-size: 16px` (or apply `@media (max-width: 768px) { input, textarea, select { font-size: 16px !important; } }`). | S |

## P1 — Significant

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 3 | `index.html:61,185,194,1724,3124,3186,3614,3680,3685` | **9 uses of `100vh` / `calc(100vh - …)`** for full-screen layout. On iOS Safari and Chrome Android the URL bar makes `vh` include hidden pixels — the bottom of the alert feed / arrest dock / operations view is clipped or hidden behind the bar. | Replace with `100dvh` (or guard `@supports (height: 100dvh)`). E.g. line 185: `min-height: 100dvh`. | S |
| 4 | `index.html:248–249` (`hexPulse`), `:78` (`gridPulse`), `:117` (`sonarScanDown`), `:262` (`shimmer`), `:351` (`dotPulse`), `:481/491` (rotate), `:504/507` (`threatPulse`), 29 `animation:` declarations total | **Zero `@media (prefers-reduced-motion: reduce)` support.** Logo pulses 3s, grid pulses 4s, sonar sweeps 4s, threat ring rotates, dots pulse 2s, shimmer text 3s — all run simultaneously and continuously. Users with vestibular issues, plus battery-saver mode, get no relief. | Add at end of `<style>`: `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; } }`. | XS |
| 5 | `index.html:611` `.source-type.national { color: var(--cyan); border-color: var(--cyan-dim); }`, `:1093` `.alert-origtext-title`, `:1031` `.alert-full-text`, `:1320` `.history-date-filter`, `:879–891` `.search-input`, multiple `var(--cyan-dim)` borders | **`--cyan-dim: #00d4ff44`** is `rgba(0,212,255,0.27)` — measured contrast against `#040810` ≈ **2.1:1**. Below WCAG AA (3:1 for non-text UI components). Decorative dividers OK; but `.lmp-radio` border (line 2135 / 2222), `.legend-circle` borders, and tooltip outlines fail. | Promote to `var(--cyan)` (#00d4ff) for any element conveying state (focused, selected, active borders). Reserve `--cyan-dim` for purely decorative glows. | M |
| 6 | `index.html:5717–5723` (`toggleSidebarMobile`), `:5575` (`switchView`) | **Mobile drawer has no keyboard escape, no focus trap, no `aria-expanded` sync, no scroll-lock.** Open the sidebar on mobile, tab key sends focus behind the overlay; opening the drawer doesn't move focus to the first nav item; `Esc` doesn't close it. Background page scrolls while drawer is open. | In `toggleSidebarMobile()`: toggle `document.body.style.overflow = open ? 'hidden' : ''`; set `aria-expanded` on the toggle button; on open, `document.querySelector('.sidebar-item.active')?.focus()`; bind one-time `document.addEventListener('keydown', e => e.key==='Escape' && close())`. | M |
| 7 | `index.html:2251–2261` `.arrest-panel` `position: absolute; top: 60px; left: 12px; width: 280px` | **Hard-coded LTR positioning in an RTL document.** The arrest planner floats over the dashboard map with `left: 12px` — in RTL contexts the visual "left" is correct only because Leaflet doesn't auto-flip, but other absolute-positioned overlays (`.map-coords` left:10px, `.map-btn-row` left:10px, `.map-label` right:10px) read as LTR coordinates. The arrest panel can overlap the map-btn-row on a narrow screen since both target left:10–12px in the same parent. | Use `inset-inline-start` instead of `left`, and bump arrest-panel to `top: 56px` (matches map-btn-row offset). On screens <1100px, ensure arrest-panel and map-btn-row don't overlap (e.g. shift arrest-panel down to `top: 96px` via `@media (max-width: 1100px)`). | S |
| 8 | `index.html:3517–3518` `.sidebar-item-badge { margin-right: auto; }` and `.history-overlay` headers (`:1112` `.vote-buttons margin-right:auto`, `:4186` modal header) | **`margin-right: auto` used to push elements to the visual end in an RTL container.** In RTL flex, this pushes the badge to the **visual left** (the *start*), not the end as authored. Same bug in history-overlay CSV button (`:4426 style="margin-right:auto"`). | Replace `margin-right: auto` with `margin-inline-start: auto` (or `margin-left: auto` for "visual right in RTL"). Audit all 11 `margin-right`/`margin-left` occurrences. | S |
| 9 | `index.html:3413–3426` `body::after` full-viewport CRT scanline with `mix-blend-mode: overlay; z-index: 9999; position: fixed` | **Always-on full-screen overlay paints behind every modal.** It's `pointer-events: none` so it doesn't block clicks, but `mix-blend-mode: overlay` on a fixed full-viewport layer forces compositing of the entire page on every scroll/animation tick — significant GPU cost on low-end phones. The mobile media query at line 3315 disables it (`body::after { display: none; }`), good — but desktop still pays the cost. | Add an option in config panel to disable, or wrap in `@media (prefers-reduced-motion: no-preference) and (min-resolution: 2dppx)` so it's only on retina desktops, never on integrated GPUs at 1×. | S |
| 10 | `index.html:1944–1947` (login-input `style` inline), `:3009` (login-input class), `:3782` (login overlay input) | **Login input has `text-align: center` and `letter-spacing: 8px` with `-webkit-text-security: disc`.** On Safari & some Chromium builds, masked characters render with kerning that pushes the visible bullets off the centered baseline; the input visually drifts during typing. Also: `text-security` is non-standard — fallback in Firefox is to show characters in clear. | Use a real `<input type="password">` for the login code (one Hebrew label says "קוד גישה" — that's exactly what password is for). Remove `text-security: disc`. Keep visual styling. | XS |

## P2 — Important

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 11 | `index.html:2291–2317` `.arrest-stepper .step` (5 children with `flex: 1`, `padding: 5px 3px`) | **Wizard stepper labels are 9 px (`small` element line 2317), in a 280 px planner dock that's 300 px in ops view.** Each step gets ~50 px of width; the Hebrew labels (`מטרה`, `מוצא`, `מסלול`, `חזרה`, `חילוץ`) just fit, but the 9 px size below WCAG recommended minimum body text and reads like fine print. | Bump `.arrest-stepper .step small { font-size: 10px; }` or `11px`, drop letter-spacing. Test on the narrowest mobile breakpoint. | XS |
| 12 | `index.html:1113` `.vote-btn { width:28px; height:28px; }`, `:996–1001` `.alert-check { width:22px; height:22px; }`, `:692–700` `.map-btn { padding:6px 12px; font-size:12px; }` | **Click targets below 44 px in the desktop layout.** Vote buttons in feed = 28 px. Alert-check checkbox = 22 px. Map buttons ≈ 32 px tall. Mobile media query (`:3293`) bumps to `min-height: 40px` but desktop is not touched, and Windows tablets / Surface devices use desktop layout. | Set `.vote-btn`, `.alert-check`, `.map-btn` to `min-width: 40px; min-height: 40px` site-wide (not just under 768px). Increase padding rather than font-size. | S |
| 13 | `index.html:4125–4127` (three map buttons) `&#x26F6;`, `&#9881;`, `&#9660;` HTML entity icons | **Unicode emoji-style icons (⛶ ⚙ ☰) render differently across OS** — on Linux/Windows they appear as line glyphs, on macOS as colored emoji. The "demo" gear (line 4126) is colored yellow via `style="color:var(--yellow)"` but macOS emoji ignores `color`. | Replace with the inline `<svg>` icons used elsewhere in the file. The arrest panel buttons (line 4175–4189) already use SVG — extract a consistent icon library, retire HTML entities. | M |
| 14 | `index.html:269–275` `.system-subtitle { font-size: 11px; font-weight: 300; color: var(--text-secondary); }`, `:736–740` `.summary-meta { color: var(--text-dim) font-size: 10px }`, `:1093` `.alert-origtext-title { font-size: 9px; color: var(--text-dim) }`, `:1106` `.alert-meta-row { font-size: 9px; color: var(--text-dim) }` | **Multiple secondary-text spots at 9–10 px combined with `--text-dim: #94aac8`.** Contrast against `--bg-panel rgba(8,16,35,0.82)` ≈ 5.5:1 (passes AA Normal), but at 9–10 px the AA threshold rises to 4.5:1 for "Large Text" which 9 px **never** qualifies for. Result: legible only at desktop monitor distance. | Promote secondary text to 11 px minimum, or to `--text-secondary` for the smallest sizes. | S |
| 15 | `index.html:1947`, `:1984`, `:9065` — multiple `position: fixed; inset: 0; z-index: 9999` ad-hoc overlays in JS-injected markup; `:3418` body::after also z-index 9999; `:3837` tactical-modal-overlay z-index: 100000 | **Z-index escalation war.** Login overlay 99999, tactical-modal 100000, body::after 9999, toast 9999 (line 5503), JS modal 9998 (line 9065), publisher 8001, history 8000, keywords 8000. Adding the next dialog requires guessing a number > all of these. | Document a z-index scale near top of `:root` block: `--z-base: 0; --z-overlay: 100; --z-modal: 200; --z-toast: 300; --z-tooltip: 400`. Refactor offenders to use these (especially line 9065 which uses 9998). | M |

## P3 — Polish

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 16 | `index.html:4426` history overlay CSV button uses `class="history-close"` despite not being a close button | Confusing class reuse; CSS `.history-close:hover { border-color: var(--red) }` makes the CSV button glow red on hover. | Add `.history-action` class with neutral hover, or override `border-color: var(--cyan)` on hover. | XS |
| 17 | `index.html:925–930` `@keyframes alertFlash { 0% { background: rgba(0,212,255,0.2); } 100% { background: transparent; } }` runs 2s on new items but `.alert-item` has `transition: background 0.3s ease` already; flashes can compound when many arrive together. | Cosmetic flicker. | Pause animations briefly when >5 items arrive in <1s (`requestIdleCallback` batch). | M |

## Positive findings

- The recent commit (`2222d8b UX: smarter save validation + fix low-contrast text across dashboard`) is visible — `--text-primary: #f0f5fb` and `--text-secondary: #c0d0e5` are properly luminous on `--bg-primary: #040810` (≈ 14:1 and ≈ 11:1, AAA).
- The 5-step arrest-wizard stepper (`:2299–2331`) uses three clean visual states (`step`, `step.active`, `step.done`) with `box-shadow` glow on active — visually unambiguous.
- The `tactical-modal` (`:3825–3954`) replaces native `confirm`/`alert` with an `Esc`-and-`Enter`-bindable promise-based modal — good UX choice, properly auto-focuses OK button.
- `prefers-reduced-motion` aside, animation timings are mostly 2–5s ease-in-out — not jarring at default speed.
- RTL `dir="rtl"` is set at the root, `direction: rtl` is repeated on inputs/panels — Hebrew typing direction works correctly throughout.
- Mobile drawer code path at `:3119–3327` is thorough — sidebar slides from the visual-right (correct RTL origin), overlay backdrop dims at 0.6, z-index 10000 explicitly clears Leaflet's 700.
- Alert items have proper `word-break: break-word` and `line-clamp: 4` for collapsed state — prevents long Hebrew sentences from overflowing.
- Loading states with `<div class="loading-spinner">` are present in all major panels (sources, summary, alert feed, keywords, discoveries) — no blank panels during load.
