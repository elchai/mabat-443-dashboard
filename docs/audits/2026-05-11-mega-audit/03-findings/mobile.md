# Mobile Responsiveness Findings — 2026-05-11

Target: `C:\Users\User\Desktop\DEV\github\mabat-443-dashboard\index.html`.
Breakpoints in source: `1200px`, `900px`, `768px`, `480px`, `420px`. Two `@media (max-width: 900px)` blocks exist (`:1717` and `:3713–3738`) and one comprehensive `@media (max-width: 768px)` block (`:3119–3327`). Mobile work has clearly been done — these findings cover what still bites on real phones.

## P0 — Critical (blocks use)

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 1 | `index.html:888` (`.search-input` 11px), `:1547` (`.report-field input` 12px), `:1303` (`.history-search` 12px), `:2436` (corner inputs 11px), `:2474` (arrest-name 11px), `:4167` target-name | **iOS auto-zoom on tap-focus.** Every form input is below 16px, so iOS Safari zooms the page to ~150% on focus and does NOT zoom back. After typing a search query or filling a corner name, the user is stuck on a misaligned, oversized layout until they manually pinch-zoom out. **This breaks the entire arrest-planner workflow on iPhone.** | Add a single rule near top of CSS: `@media (max-width: 768px) { input, textarea, select { font-size: 16px !important; } }`. Visual hierarchy is preserved via padding, not type size. | S |
| 2 | `index.html:61,185,194,1724,3124,3186,3211,3240` — 9 uses of `100vh` (also `calc(100vh - 80px)`, `calc(100vh - 120px)`) | **`100vh` includes the iOS URL bar.** On the operations view (line 3211: `min-height: 55vh`, line 3240: `height: 55vh` for the ops-map-wrap), and on the dashboard view at line 3614 (`min-height: calc(100vh - 80px)` for map-panel), the bottom of the page is hidden behind the address bar. The arrest planner dock's save buttons get clipped off-screen. | Switch every `100vh` to `100dvh` (dynamic-viewport). Ship a fallback: `min-height: 100vh; min-height: 100dvh;`. Affected lines: 61, 185, 194, 1724, 3124, 3186, 3614, 3680, 3685 + the `55vh` / `45vh` / `35vh` map-panel heights. | S |

## P1 — Significant

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 3 | `index.html:2299–2331` `.arrest-stepper` 5 steps × `flex: 1` × `padding: 5px 3px` | **5-step wizard inside 280–300 px planner dock.** Each step gets ~50 px width with a 9 px Hebrew label (`small` line 2317). On phones the planner-dock becomes full-width (line 3245), so the labels become legible — **but on tablet portrait (768–900 px range) the dock is still 300 px and labels are cramped.** Verified: the labels `מטרה / מוצא / מסלול / חזרה / חילוץ` fit but the active "glow" `box-shadow: 0 0 8px` (line 2329) bleeds into neighbors. | Add `@media (max-width: 900px) { #view-operations .ops-planner-dock { width: 100% !important; max-height: 50vh; } }` — currently this only kicks in at 768 px (line 3245). Or stack the 5 steps as 2 rows of 3 on narrow widths. | M |
| 4 | `index.html:4124–4128` map-btn-row with 3 buttons, `:3304–3309` mobile override `top: 6px !important; left: 6px !important; gap: 4px;` | **Map controls overlap the arrest-panel** which opens at `top: 60px; left: 12px` (line 2253). On mobile the panel becomes `position: static` (line 3270), so the conflict disappears for the dashboard map — but on the **operations view** the arrest panel is in the dock (line 4357), and the map's `map-btn-row` still floats top-left of the map. On a phone, the 3 buttons (`הרחב`, `הדגמה`, `שכבות`) wrap to 2 lines and steal ~50 px of vertical map space — fine, but they sit above the Leaflet zoom control at top-right (zoom is top-right by default in RTL? actually Leaflet zoom is top-left by default → direct collision). | Verify with DevTools: if zoom and `.map-btn-row` are both at top-left, set Leaflet `zoomControl: false` in JS and add a custom right-side zoom, or move `.map-btn-row` to top-right on mobile via `right: 6px; left: auto;`. | S |
| 5 | Leaflet default controls (zoom +/−) ~30×30 px — no override CSS found | **Leaflet zoom buttons are 30 px tall** (Leaflet default), below the 44 px Apple HIG / 48 dp Material target. Pinch-zoom mostly substitutes, but tactile users (gloves, cold fingers, accessibility) miss-tap. | Add `@media (max-width: 768px) { .leaflet-touch .leaflet-bar a { width: 44px !important; height: 44px !important; line-height: 44px !important; font-size: 22px !important; } }`. | XS |
| 6 | `index.html:4228–4237` alert-toolbar — 8 filter-chip buttons + search input | **Mobile (`:3285–3290`)** sets `overflow-x: auto` and `flex-wrap: nowrap` — chips scroll horizontally. **But the search input** at line 4237 has no width constraint inside the scrolling container; it can either (a) push other chips off-screen, or (b) compress to ~80 px making search useless. There's no visual hint that the chips scroll. | Wrap chips in their own scroll container, pull the search input out into a separate row below on mobile. Add a subtle fade-out gradient on the right edge to signal scroll-ability: `mask-image: linear-gradient(to left, black 90%, transparent 100%)`. | M |
| 7 | `index.html:3128–3137` mobile header — `flex-wrap: nowrap` + `padding: 6px 10px` + `flex: 1; min-width: 0` on logo-section | **Header on iPhone SE (375 px wide)** packs: logo (28 px), system-title (14 px font, 5 letters with 2 px tracking → ~110 px), menu button (44 px), gaps (24 px). That's ~210 px; remaining ~165 px for clock+date (hidden via line 3154) and nothing else. **The header is OK but cramped on 320 px Galaxy Fold (closed).** | Add `@media (max-width: 360px) { .system-title { font-size: 12px !important; letter-spacing: 1px !important; } }`. Currently the 420 px breakpoint only hides the subtitle. | XS |

## P2 — Important

| # | file:line | Issue | Fix | Effort |
|---|---|---|---|---|
| 8 | `index.html:1508–1517` `.report-form { width: 90%; max-width: 420px; padding: 25px; }` — no `max-height` | **Field report modal overflows viewport in landscape phone.** 7 fields (title, details textarea, urgency row, location, reporter, 2 buttons) at default padding don't fit in a 360 px landscape window. The overlay scrolls the **outer** `.report-overlay` which has `display: flex; align-items: center` — so the modal centers and the **top** is unreachable. | Change `.report-overlay` to `align-items: flex-start; padding: 20px 0`. Set `.report-form { max-height: 90dvh; overflow-y: auto; }`. | S |
| 9 | `index.html:3293–3295` mobile touch-target rule: `button, .map-btn, .filter-chip, input[type="radio"]+span, .sidebar-item { min-height: 40px; }` | **`min-height: 40px` is below the recommended 44 px (iOS) / 48 dp (Android).** Especially the filter chips (`:1774` size 9 px on mobile, height 40 px from this rule) — they're tall enough but their visible color band is thin so users mis-tap to neighbors. | Bump rule to `min-height: 44px`. For filter chips, also add `padding: 10px 8px` to broaden the hit area to match the visible chip. | XS |
| 10 | `index.html:3211` `#view-dashboard .map-panel { min-height: 55vh !important; }`, `:3839` (480 px) `.map-panel { height: 35vh; min-height: 220px; }` | **Conflicting map heights.** The 768 px rule sets `min-height: 55vh`, the 480 px rule sets `height: 35vh` and `min-height: 220px`. On a 480 px-wide screen, both rules apply and CSS precedence + `!important` means the 768 px rule wins → map is 55vh, not 35vh. Likely the intent was "smaller map on tiny phones to give more room to alerts" — but it doesn't happen. | Either drop the 55vh `!important` at line 3211, or restate `min-height: 35vh !important` in the 480 px block (`:1834`). Confirm tested on 414 px (iPhone 11) and 360 px (Galaxy A series). | XS |

## Positive findings

- **Sidebar drawer is properly implemented** for mobile: line 3173–3190 — slides in from RTL-right origin, z-index 10000 clears Leaflet's 700, overlay at 9999, smooth `cubic-bezier(0.4,0,0.2,1)` transition.
- **Hamburger toggle has 44×44 px tap target** (line 3158–3170) — meets HIG.
- **Filter chips & data tabs scroll horizontally** (line 3260–3265, 3285–3290) with `-webkit-overflow-scrolling: touch` for iOS momentum.
- **`body::after` CRT scanline is disabled on mobile** (line 3315 `body::after { display: none; }`) — saves ~5–10% GPU on low-end Android.
- **Map dock layout flips to vertical** on mobile in operations view (line 3228–3256) — map on top, planner dock below — correct mental model for thumb reach.
- **Arrest exports become a 2-column grid** on mobile (line 3275–3281) and single-column under 420 px (line 3330–3333) — adaptive.
- **`.tactical-modal` is `width: 92vw`** on mobile (line 3298–3301) — sensible.
- **Login overlay sized properly for mobile** (line 3318–3322) — 92vw, padding adjusted.
- **Sidebar-mobile-toggle is set to `order: -1`** in RTL flex (line 3169) — visually anchors to the same side the drawer slides in from. Thoughtful RTL handling.
- **`flex-direction: column` on `#view-dashboard .left-panel`** on mobile (line 3214) instead of horizontal-scroll cards — the comment "stacked, not horizontal scroll — wastes less space" shows intentional UX choice.
